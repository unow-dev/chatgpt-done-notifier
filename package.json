{
  "name": "chatgpt-done-notifier",
  "version": "1.4.1",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "esbuild": "^0.23.0",
    "typescript": "^5.6.3",
    "chrome-types": "^0.1.269"
  }
}
